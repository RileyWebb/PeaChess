#include "client/ui.h"
/*
ui_svg_t *UI_SVGCreate(char *src) 
{
    ui_svg_t *svg = malloc(sizeof(ui_svg_t));


}

void UI_SVGDraw(ui_svg_t *svg) 
{
    ImDrawList list;

    for (size_t i = 0; i < svg->path_count; i++)
    {
        ui_svg_path_t *path = &svg->paths[i];

        switch (path->type)
        {
        case UI_SVG_PATH_TYPE_LINE:
            ImDrawList_AddLine(&list, path->p1, path->p2, path->color, path->thickness);
            break;
            ImDrawList_fill
        default:
            break;
        }
    }
    
}*/